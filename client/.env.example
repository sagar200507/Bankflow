# ========================
# CLIENT ENVIRONMENT
# ========================
# Vite requires VITE_ prefix for all client-exposed variables.
# These are compiled into the bundle at build time — NEVER put secrets here.

VITE_API_BASE_URL=http://localhost:5000/api/v1
VITE_APP_NAME=BankFlow
